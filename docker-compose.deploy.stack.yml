version: '3.7'
services:
  backend:
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
  celeryworker:
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
  celerybeat:
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
