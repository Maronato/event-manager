version: '3.7'
services:
  backend:
    volumes:
      - ../backend/.env:/app/.env
  celeryworker:
    volumes:
      - ../backend/.env:/app/.env
  celerybeat:
    volumes:
      - ../backend/.env:/app/.env
  proxy:
    deploy:
      placement:
        constraints:
          - node.role == manager
